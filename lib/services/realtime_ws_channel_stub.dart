import 'package:web_socket_channel/web_socket_channel.dart';

WebSocketChannel connectRealtimeWebSocketImpl({
  required Uri uri,
  required String apiKey,
}) {
  throw UnsupportedError('Realtime WebSocket is not supported here');
}

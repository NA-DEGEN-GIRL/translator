import 'package:web_socket_channel/web_socket_channel.dart';

WebSocketChannel connectOpenAIWebSocketImpl({
  required Uri uri,
  String? apiKey,
}) {
  throw UnsupportedError('OpenAI WebSocket is not supported here');
}
